// Legacy Expo babel config — kept as a no-op so any tooling that probes for it
// doesn't crash. Vite + React in /app/ uses its own toolchain (esbuild/SWC) and
// does not consume this file.
module.exports = { presets: [], plugins: [] };
